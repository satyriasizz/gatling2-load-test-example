service {

    # url to host:
    host = "http://localhost:8080"

    # url to api:
    api_link = "/customer"
}

scenario {

    # Scenario repeat count:
    repeat_count = 2

    # Emulate the certain count of users for simulation:
    thread_count = 2
}